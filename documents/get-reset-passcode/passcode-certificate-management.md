# Passcode & Certificate Management - Complete Solution

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        PASSCODE & CERTIFICATE LIFECYCLE                              │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                   USER SCENARIOS                                     │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────────────────┐   │
│  │  FORGOT PASSCODE │    │ CERT EXPIRING    │    │ CERT EXPIRED                 │   │
│  │                  │    │ (≤5 days left)   │    │                              │   │
│  └────────┬─────────┘    └────────┬─────────┘    └──────────────┬───────────────┘   │
│           │                       │                              │                   │
│           ▼                       ▼                              ▼                   │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────────────────┐   │
│  │ POST /user/      │    │ Show Warning     │    │ Block approval actions       │   │
│  │ passcode         │    │ Banner/Modal     │    │ Force regenerate             │   │
│  │                  │    │                  │    │                              │   │
│  │ → Get existing   │    │ → Prompt user to │    │ POST /user/passcode/         │   │
│  │   passcode email │    │   regenerate     │    │ regenerate                   │   │
│  └──────────────────┘    └──────────────────┘    └──────────────────────────────┘   │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              SYSTEM ARCHITECTURE                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐      ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  FRONTEND   │      │    BFF      │      │  Employee   │      │    EIA      │
│  (React)    │      │  (8071)     │      │  ADC (8092) │      │   (8093)    │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │                    │
       │  GET /user/certificate/status          │                    │
       │───────────────────►│                    │                    │
       │                    │  GET /certificates/status/{empId}      │
       │                    │───────────────────►│                    │
       │                    │◄───────────────────│                    │
       │◄───────────────────│                    │                    │
       │  { isExpired, isExpiring,              │                    │
       │    daysUntilExpiry, validUntil }       │                    │
       │                    │                    │                    │
       │                    │                    │                    │
       │  [If forgot passcode]                  │                    │
       │  POST /user/passcode                   │                    │
       │───────────────────►│                    │                    │
       │                    │                    │  POST /job         │
       │                    │                    │  (emailEmployeePasscodeJob)
       │                    │───────────────────────────────────────►│
       │                    │                    │                    │──► AWS SES
       │◄───────────────────│                    │                    │    → Email
       │  { message: "sent" }                   │                    │
       │                    │                    │                    │
       │                    │                    │                    │
       │  [If cert expiring/expired - user clicks regenerate]       │
       │  POST /user/passcode/regenerate        │                    │
       │───────────────────►│                    │                    │
       │                    │  POST /certificates/employees/{id}/certificate
       │                    │───────────────────►│                    │
       │                    │◄───────────────────│ (new cert created) │
       │                    │                    │  POST /job         │
       │                    │───────────────────────────────────────►│
       │                    │                    │                    │──► AWS SES
       │◄───────────────────│                    │                    │    → Email
       │  { message: "regenerated" }            │                    │
       │                    │                    │                    │


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND NOTIFICATION FLOW                                   │
└─────────────────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────────┐
                              │   User Login    │
                              └────────┬────────┘
                                       │
                                       ▼
                       ┌───────────────────────────────┐
                       │  Fetch Certificate Status     │
                       │  GET /user/certificate/status │
                       └───────────────┬───────────────┘
                                       │
                        ┌──────────────┴──────────────┐
                        ▼                             ▼
               ┌────────────────┐            ┌────────────────┐
               │  Has Valid     │            │  No Certificate │
               │  Certificate?  │            │  or Expired?    │
               └───────┬────────┘            └────────┬───────┘
                       │                              │
            ┌──────────┴──────────┐                   │
            ▼                     ▼                   ▼
    ┌──────────────┐      ┌──────────────┐   ┌──────────────────┐
    │ daysLeft > 5 │      │ daysLeft ≤ 5 │   │ Show Error Modal │
    │              │      │              │   │ "Certificate     │
    │ Normal flow  │      │ Show Warning │   │  Expired"        │
    │ No banner    │      │ Banner       │   │                  │
    └──────────────┘      └──────┬───────┘   │ [Regenerate Now] │
                                 │           └────────┬─────────┘
                                 │                    │
                                 ▼                    │
                    ┌────────────────────┐            │
                    │ Warning Banner:    │            │
                    │ "Certificate       │            │
                    │  expires in X days"│            │
                    │                    │            │
                    │ [Renew Now] button │            │
                    └─────────┬──────────┘            │
                              │                       │
                              ▼                       ▼
                    ┌─────────────────────────────────────┐
                    │  User clicks [Renew/Regenerate]     │
                    │  POST /user/passcode/regenerate     │
                    └─────────────────┬───────────────────┘
                                      │
                                      ▼
                    ┌─────────────────────────────────────┐
                    │  Success: Show confirmation         │
                    │  "New passcode sent to email"       │
                    │  Refresh certificate status         │
                    └─────────────────────────────────────┘
```

## API Endpoints Summary

| Endpoint                   | Method | Description                              | Status   |
|----------------------------|--------|------------------------------------------|----------|
| `/user/passcode`           | POST   | Send existing passcode to user's email   | ✅ Done  |
| `/user/passcode/reset`     | POST   | Reset password, keep cert, send email    | ✅ Done  |
| `/user/passcode/regenerate`| POST   | New certificate + passcode, send email   | ✅ Done  |
| `/user/certificate/status` | GET    | Get cert expiry status for frontend      | 🆕 NEW   |

## NEW ENDPOINT: GET /user/certificate/status

### Request
```
GET /api/user/certificate/status
Authorization: Bearer <JWT>
```

### Response (200) - Has Valid Certificate
```json
{
  "hasCertificate": true,
  "isActive": true,
  "isExpired": false,
  "isExpiring": true,
  "daysUntilExpiry": 3,
  "validFrom": "2024-01-15T00:00:00",
  "validUntil": "2025-01-15T00:00:00",
  "certificateId": 123,
  "message": "Certificate expires in 3 days"
}
```

### Response (200) - No Certificate
```json
{
  "hasCertificate": false,
  "isExpired": true,
  "isExpiring": false,
  "daysUntilExpiry": 0,
  "message": "No valid certificate found"
}
```

## Frontend Implementation Guide

### 1. Certificate Status Hook (`useCertificateStatus.ts`)
- Fetch status on app load / login
- Store in Redux or React Context
- Re-fetch periodically (every hour) or on specific actions

### 2. Warning Banner Component
- Show when `daysUntilExpiry ≤ 5`
- Yellow/orange warning color
- Message: "Your certificate expires in X days. [Renew Now]"

### 3. Error Modal Component
- Show when `isExpired = true`
- Red error styling
- Block approval actions until regenerated
- Message: "Your certificate has expired. Please regenerate to continue."
- Button: [Regenerate Certificate]

### 4. Integration Points
- `App.tsx`: Check status on mount
- Approval pages: Check before allowing approval
- Header/Navbar: Show warning badge

## Backend Implementation

### File: `UserInfoController.java`

```java
@GetMapping("/user/certificate/status")
public ResponseEntity<?> getCertificateStatus() {
    UserContext user = getCurrentUser();
    // Call ADC /certificates/status/{employeeId}
    // Transform response with isExpiring calculation (≤5 days)
    // Return CertificateStatusDTO
}
```

## Frontend Files to Create/Modify

1. `src/api/certificateApi.ts` - API calls
2. `src/hooks/useCertificateStatus.ts` - Status hook
3. `src/components/CertificateWarningBanner.tsx` - Warning banner
4. `src/components/CertificateExpiredModal.tsx` - Expired modal
5. `src/features/layout/Header.tsx` - Add warning indicator
6. `src/app/App.tsx` - Initialize status check